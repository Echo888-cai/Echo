{
  "name": "@echo/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "R-0 contract layer: zod schemas + OpenAPI snapshot describing Echo Research's current HTTP API (src/server/routes/*.js). Descriptive only — not a spec for future behavior.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "generate-openapi": "tsx src/generate-openapi.ts",
    "test": "tsx --test src/contract-tests/*.test.ts"
  },
  "dependencies": {
    "zod": "^3.24.1",
    "@asteasolutions/zod-to-openapi": "^7.3.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "tsx": "^4.19.2"
  }
}
