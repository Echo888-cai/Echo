# Echo Research 主计划 v2：Product + Research Terminal Gap Audit（2026-07-05）

> **这是什么**：Echo Research 的**唯一权威计划文档**（v2）。取代 v1（EA/B/D 主计划，其全文与状态表见 git history 中的本文件）。v1 规划的三条主线——EA-0…EA-5 统一 Agent、B-1…B-7 研究质量深化、D1…D3 架构加固——已全部完成并验收；C1 的移动端阻断 bug 已顺手修复（C1/C2 余项保持 🟡 顺手策略）。
> **怎么来的**：用户明确要求在进入任何新功能（含 P8 商业化、EA-6 自定义 skill）之前，先做一次系统性 Gap Audit——从①专业投资研究员、②产品体验、③工程与数据三个视角全面审视产品还缺什么，先对齐方向再开工。本文档就是这次审计的产出：证据基础（§1）→ 逐条 gap（§2）→ 建议路线（§3）。
> **给谁看**：任何接手的人（人类或新 AI 会话）。自包含，不需要先读其他文档。
> **状态**：§2/§3 是**建议**，尚未与用户对齐开工。对齐后把选中的阶段填进 §5 状态表再动手。

---

## 0. 一段话现状（接手先读这条）

Echo 已经是一个能自圆其说的单机研究终端：统一入口 `/api/ask`（意图路由 + 受控对比规划器）、宪法级研究纪律（`src/prompts.js`）、结构化决策面板（评级/估值条/证伪/溯源/置信度，置信度有事实锚定护栏）、stage-aware 估值引擎（亏损股 EV/Sales + 脏数据抑制）、港美一手数据管道（SEC 8-K 结构化抽取 + HKEX PDF 三表，B-6 加固过复杂版式）、对话即容器（会话分组 + 多公司 Tab + 全标的自动进看盘）、主动性（scheduler 盘前速报/触线/证伪巡检 + 通知中心 + Telegram）。工程底座：CI（lint+263 测试）+ `npm run typecheck` 全量 0 error + 迁移器（`PRAGMA user_version`）+ 前后端全模块化。

**但按"真正可用的港美股 AI research desk"的标准衡量，短板集中在三处**：研究缺"锚"（业绩日历、同业估值对照、数字级防幻觉校验都没有）；数据可信度不可观测（免费源脆弱是常态，降级却对用户和开发者都不可见）；测试体系与真实世界脱节（263 条测试全部无网络，而本项目两个最重要的 bug 都是真实调用/静态分析抓到的）。§2 逐条展开。

---

## 1. 审计方法与证据基础

三视角审读了以下材料（结论都以 file:line 为证，不凭印象）：

- **计划与文档**：v1 PLAN 全文（含 EA/B/D 全部验收记录）、DATA_SOURCE_STRATEGY、README。
- **研究链路源码**：`dataSources.js`（并发拉数 + 超时降级 + B-5 港股合并）、`valuationEngine.js`（全部估值方法与护栏）、`decisionPanel.js`（证据结构 + 置信度护栏）、`chatOrchestrator.js`（对话编排全链）、`discovery.js`（筛选/宏观）、`intentClassifier.js`、`agentPlanner.js`、`prompts.js`（宪法 + 8 框架）。
- **数据与基建**：`market.js`/`marketData.js`/`financialData.js`/`newsData.js`/`fmpClient.js` 函数清单、`hkFilingsPipeline.js`、`modelGateway.js`（GLM→DeepSeek→OpenAI failover）、`scheduler.js`、`migrations/001_init.sql`（14 表）、`scripts/doctor.js`、CI workflow。
- **前端**：`src/ui/*` 13 模块（含空状态/引导现状）、`tests/` 21 个文件的覆盖面。

已完成轨道的关键结论（影响 gap 定级）：B 轨证明了"真实数据驱动 + fail-safe 优于假数据"（B-5/B-6）；B-7 与 D1 两次证明**纯单测全绿也抓不到真实世界的 bug**（搜索日期污染、`hasPrice` 当除数）——这条经验直接推高了 E1（canary）的优先级。

---

## 2. Gap 清单（三视角，每条含七要素）

> 字段约定：**问题 / 为什么重要 / 用户价值 / 工程风险 / 建议优先级 / 需要真实数据验证? / 预计工作量**。
> 优先级：P1 = 建议下一程就做；P2 = 第二程候选；P3 = 记录在案等时机；P4 = 明确缓做。

### 视角一 · 专业投资研究员（R：距离真正的 research desk 差什么）

**R1 · 财报日历缺位——系统不知道"下一份财报什么时候来"** ★
- **问题**：全链路没有任何 earnings calendar 数据源。决策面板的 action 高频输出"等财报/公告验证"（`decisionPanel.js` `composeAction`），scheduler 有盘前速报/证伪巡检，但没有任何组件知道下一个业绩日在哪天。
- **为什么重要**：研究桌的工作节奏是围绕业绩日组织的——业绩日前提醒"你的证伪条件即将被检验"，业绩日后触发"指引 vs 实际"复核。没有日历，"等财报验证"是一句永远没有下文的话。
- **用户价值**：高。直接强化已有闭环（研究→证伪条件→监控→提醒），看盘卡可显示"下一业绩日 T-5"。
- **工程风险**：低。Finnhub `/calendar/earnings` 免费档覆盖美股；港股无免费日历，可从 HKEX 公告节奏近似或诚实标"未核到"（fail-safe 原则）。
- **优先级**：P1 ｜ **真实数据验证**：需要（Finnhub 免费档字段/配额、港股可得性实测）｜ **工作量**：≈1 天。

**R2 · 估值缺同业与历史锚——宪法要求"与历史区间和同业匹配"，引擎给不出** ★
- **问题**：PE 法用"当前 PE ±30%"当 bear/bull（`valuationEngine.js:306-311`），本质自参照；EV/Sales 用增速分档的**规则默认倍数**（`:185-187`），不看真实同业。宪法估值纪律（`prompts.js` 第五条）要求说明"与历史区间和同业是否匹配"，但引擎没有任何同业倍数或历史分位数据——这段纪律只能靠模型脑补，恰是事实块红线要防的行为。
- **为什么重要**：估值条/赔率是产品核心卖点，"±30% 机械带"在专业用户眼里一眼假；同业锚也是"贵不贵"唯一有说服力的语境。
- **用户价值**：高。估值带从"机械带"变成"有对照的带"，散文与数字终于同源。
- **工程风险**：中。同业对标可落地（`SECTOR_MAP`/curated 名单/本地 companies 池选 2-3 家同业拉 PE/EV-S 取中位，对比管道 `buildCompareSummary` 现成）；**自身历史估值分位免费档拿不到**（需历史 PE 序列）——先做同业、历史分位诚实标"未核到"。
- **优先级**：P1 ｜ **真实数据验证**：需要（同业倍数免费档覆盖度实测）｜ **工作量**：≈2 天。

**R3 · 事实块红线只是提示词，没有数字级代码校验**
- **问题**：宪法禁止模型给出事实块之外的财务数字，但没有任何代码校验模型散文里的数字是否真在已接地数据里。B-1 的 `reconcileConfidence` 只在"面板置信度"粒度工作，防不了正文里编一个"收入约 2-3 亿"。
- **为什么重要**：防幻觉是产品第一信任支柱；提示词纪律会随模型版本/回答长度漂移，只有代码校验可靠——与 B-7/D1 的经验同构。
- **用户价值**：高但不可见（价值在避免一次信任崩塌）。
- **工程风险**：中高。数字抽取/匹配的**误报**会把合法推演误标（汇率折算、单位换算、"约十年"这类非财务数），必须用真实模型输出反复调阈值，宁可漏报不可误报。
- **优先级**：P2（先有 G-1 稳定的事实块口径与观测底座再做）｜ **真实数据验证**：必须（真实研究回答标注）｜ **工作量**：≈2-3 天。

**R4 · 对比缺"自动可比公司"**
- **问题**：对比要用户自己点名两边（compareWith / EA-2 比较句式）；研究员会主动给同业对照。单公司研究报告里没有"同业 2-3 家关键倍数对照"块。
- **为什么重要**：没有对照系，"贵不贵"没有语境；与 R2 同源同数据。
- **用户价值**：中高 ｜ **工程风险**：低中（行业结构已有；FMP peers 端点免费档需实测）。
- **优先级**：P2（与 R2 同期做可摊薄成本）｜ **真实数据验证**：需要 ｜ **工作量**：≈1 天（若与 R2 同期）。

**R5 · 组合分析停留在"体检"，缺研究联动**
- **问题**：`portfolioReview` 有集中度/止损/触线/回撤，但没有行业/市场/币种暴露，也不联动研究结论——比如"你 2 笔持仓的证伪线距现价 <5%""3 笔持仓集中在同一赛道"。持仓页只是列表 + 体检卡。
- **为什么重要**：研究→持仓是产品闭环的收口；用户点名的候选方向之一。
- **用户价值**：中高（对有持仓的用户是高）｜ **工程风险**：低（全部本地数据派生，无新数据源）。
- **优先级**：P2 ｜ **真实数据验证**：不需要 ｜ **工作量**：≈2 天。

**R6 · 管理层指引没有结构化沉淀**
- **问题**：8-K 抽取（美股）与 HKEX 解析拿到的是"事件/三表"，但**指引数字**（下季收入/毛利率指引）没有独立落库；证伪条件锚指引数字靠模型当轮记忆，跨会话即丢，"指引 vs 实际"无法自动核对。
- **为什么重要**：guidance vs actual 是研究员判断"预期差"的主工具；宪法要求"当前倍数对应哪一年预期"。
- **用户价值**：高但兑现慢 ｜ **工程风险**：高（抽取准确率/口径统一/期次对齐都难）。
- **优先级**：P3（先用 R1 把业绩节奏立起来，指引结构化等真实使用反馈再上——防过早抽象）｜ **真实数据验证**：必须 ｜ **工作量**：≈3+ 天。

### 视角二 · 产品体验（P：哪里还不像一个真正的研究终端）

**P1 · 数据源健康对用户（和开发者）不可见** ★
- **问题**：接地条只说"这一轮新闻✗"，不说"新闻源这周一直挂"。`doctor` 是 CLI 且默认不发网络请求；设置页只有 scheduler 状态。当前研究的港股有没有一手 HKEX 数据、上次后台刷新是否失败，全都无处可看。
- **为什么重要**：免费源体系天然脆弱（FMP 402、Yahoo 403、腾讯魔法字段位），**不可见的降级会被读成"产品不行"；可见的降级反而是诚实卖点**——与"诚实置信度"的品牌承诺一致。
- **用户价值**：高 ｜ **工程风险**：低（读已有 providerStatus/E1 canary 结果，纯展示层）。
- **优先级**：P1（与 E1/E3 同一阶段交付）｜ **真实数据验证**：需要（面板数据本身来自真实探测）｜ **工作量**：与 E1/E3 合计 ≈2-3 天。

**P2 · 证伪通知回链停在 ticker 级**
- **问题**：`watch_rules` 落库时记了 `session_id`（当时定下证伪条件的那轮研究），但通知点开只带 `data-ticker`（`notifications.js:99`）——用户收到"证伪命中"后回不到"当时为什么定这条线"的上下文。
- **为什么重要**：证伪闭环的最后一步是"复核当时的逻辑"；回链断了，闭环就只剩提醒。
- **用户价值**：中高 ｜ **工程风险**：低（数据都在，只差前端跳转路径）。
- **优先级**：P2 ｜ **真实数据验证**：不需要 ｜ **工作量**：≈0.5 天。

**P3 · 研究历史没有全文检索**
- **问题**：侧栏按对话分组列历史，但没有跨公司/跨会话搜索。"我上次对 X 的判断"单公司有画像 Tab 兜着，跨公司问题（"我研究过哪些液冷相关的票"）没有入口。研究沉淀越多此缺口越痛。
- **优先级**：P3（SQLite FTS5 低成本可落地）｜ **真实数据验证**：不需要 ｜ **工作量**：≈1 天。

**P4 · 移动端 C1 余项 + C2 PWA**
- **现状**：阻断性横向溢出 bug 已修（2026-07-05）；剩触控区加大、侧栏抽屉化、composer 固定底、manifest + service worker。用户已明确保持 🟡 顺手策略、不急。
- **优先级**：P3 ｜ **真实数据验证**：需要真机/多档宽度实测 ｜ **工作量**：≈2 天。

**P5 · Onboarding / 首次体验**
- **现状盘点**：其实不差——研究页空状态有品牌 hero + 4 张示例卡 + 能力 pill（`research.js:775-800`），看盘/持仓都有空状态引导 CTA。真正缺的是"第一次看到决策面板"时的术语解释（评级体系/赔率/证伪是什么）。
- **为什么缓做**：当前唯一用户是作者本人，onboarding 的 ROI 在接近有真实外部用户时才兑现。
- **优先级**：P4（明确缓做，防止误把它当低垂果实）｜ **工作量**：≈1-2 天（到时再估）。

### 视角三 · 工程与数据（E：高风险盲点）

**E1 · 测试体系没有"真实世界"层** ★
- **问题**：263 条测试全部纯函数/本地兜底（CI 明确"不需要任何 API key"）。但本项目两个最重要的 bug——B-7 搜索日期污染、D1 `hasPrice` 当除数——都是**真实调用/静态分析**抓到的，纯单测全绿也发现不了。数据管道的真实失效模式（源改版、配额耗尽、字段漂移、腾讯 `fields[3]/[32]` 魔法索引挪位）现在的暴露渠道是"用户下次研究时踩到"。
- **改法方向**：`npm run canary`——对 1 港股 + 1 美股跑全数据管道（行情/财报/新闻/评级/filing/搜索），输出每源状态矩阵并落库（供 P1 面板展示）；本机手动跑或 scheduler 每日一跑。**不进 CI**（CI 无 key，也不该烧配额）。
- **用户价值**：间接但根本（所有研究质量都坐在这些源上）｜ **工程风险**：低。
- **优先级**：P1 ｜ **真实数据验证**：本身就是 ｜ **工作量**：≈1-2 天。

**E2 · Python/pdfminer 依赖是静默盲点**
- **问题**：港股一手管道 shell 出 `scripts/extract_pdf_text.py`（pdfminer），但 `doctor` 的 9 项能力检查**没有这一项**；`refreshHkFinancialsInBackground` fire-and-forget，机器没装 python/pdfminer 时港股一手数据**静默永远缺失**——用户只会看到"港股估值又机械了"，没人知道为什么。
- **优先级**：P1（小活，随 G-1 顺手）｜ **真实数据验证**：需要本机验证 ｜ **工作量**：≈0.5 天（doctor 加检查 + 后台刷新失败留痕到面板/通知）。

**E3 · HKEX filing 覆盖率没有度量**
- **问题**：seed 库 654 支港股，实际有一手三表的只有个位数（`.cache/filings` 5 家：0700/9988/1299/9868 + AIA 被 fail-safe 拒收）。B-6 加固了解析器，但"看盘里的港股有多少家有一手数据、哪些解析失败、失败原因"没有任何统计——B-5 证明一手数据显著提升港股估值质量，却不知道这个优势覆盖了百分之几的实际使用。
- **优先级**：P1（并入 G-1 面板：覆盖率 + 失败清单）｜ **真实数据验证**：需要 ｜ **工作量**：≈1 天。

**E4 · llm_audit 调用留痕缺失**
- **问题**：`modelGateway` 不留痕（P8 已点名"计费盲区"）。现在的代价是运维盲区：GLM→DeepSeek→OpenAI failover 实际谁在接、各自延迟/失败率、慢答该怪模型还是数据管道，全靠 console 猜。
- **优先级**：P2（一张表 + gateway 打点；顺手为 P8 铺路）｜ **真实数据验证**：不需要 ｜ **工作量**：≈0.5-1 天。

**E5 · typecheck 没进 CI**
- **问题**：D1 把全量推到 0 error 但故意只留本地（当时合理——错误尚未清零）。现在 0 error 了，不进 CI 就会回潮，D1 的投入会贬值。
- **优先级**：P1（10 分钟顺手项）｜ **真实数据验证**：不需要 ｜ **工作量**：10 分钟。

**E6 · 缓存策略三套并存、目标未兑现**
- **问题**：fmpClient 内存 TTL（重启即失，而后端无热重载、开发期频繁重启）/ `market_snapshots` 落库 / `web_evidence` 落库，三套机制各自为政；DATA_SOURCE_STRATEGY 定的目标（交易时段 15 分钟、非交易 4 小时）没有系统兑现。
- **优先级**：P3（现阶段单用户体感不明显；G-1 面板会把缓存命中可视化，届时再决定要不要统一）｜ **工作量**：≈1-2 天。

**E7 · curated 赛道名单会腐烂**
- **问题**：`SECTOR_MAP` 硬编码美股龙头名单（光模块/液冷/存储/EDA/设备），无港股对应条目；并购/退市/新龙头不会自动更新。
- **优先级**：P3（记录在案；等框架注册表/EA-6 有真实需求时一起变成可配置数据）｜ **工作量**：维护性质，单次 ≈0.5 天。

---

## 3. 下一阶段建议路线（待用户对齐后开工）

**建议下一程 = G 轨：数据可信度与研究锚点**（Gap-driven，三阶段，每阶段独立可验收）：

| 阶段 | 内容 | 合并的 gap | 预估 |
|------|------|-----------|------|
| **G-1** | 数据质量面板 + 真实数据 canary：`npm run canary` 全管道探测落库 → 设置页/独立页渲染每源健康 + HK filing 覆盖率 + 失败清单；doctor 补 python/pdfminer 检查；后台刷新失败留痕 | E1 + E2 + E3 + P1 | ≈3 天 |
| **G-2** | 财报日历与业绩事件锚：Finnhub earnings calendar 接入 → 看盘卡"下一业绩日"、scheduler 业绩日前提醒（联动已有证伪条件） | R1 | ≈1 天 |
| **G-3** | 估值同业锚 + 自动可比公司：同业 2-3 家关键倍数对照进估值假设与研究正文；单公司报告自动带同业对照块 | R2 + R4 | ≈2-3 天 |
| 顺手 | typecheck 进 CI | E5 | 10 分钟 |

**为什么是这个顺序**：三个视角的 P1 汇聚点都是"可信度"——研究员缺锚（R1/R2）、产品缺可见性（P1）、工程缺真实世界反馈（E1/E2/E3），而且 G-1 是 G-2/G-3 的观测底座（新接的日历/同业数据源第一天就进健康面板）。这也延续了项目已验证的原则：真实数据 + fail-safe + 真实调用测试。

**第二程候选**（G 轨完成后再对齐，不预设）：
- **组合/持仓分析增强**（R5 + P2 通知回链）：闭环收口方向；
- **数字级事实锚定护栏**（R3）：信任深化方向，依赖 G-1 的观测底座；
- **llm_audit**（E4）：运维/商业化铺路，可作为任意一程的顺手项。

**明确不动的**（本轮用户已确认）：P8 商业化（等产品价值更稳）、EA-6 用户自定义 skill（等真实需求）、C1/C2 余项（保持 🟡 顺手策略）、P5 onboarding（等接近有真实用户）。

---

## 4. 明确不做（防后人走弯路，承接 v1 全部红线）

1. **不换前端框架**：vanilla ESM + 事件委托，EA-5 已证明可维护性够用。
2. **不做完全自主 ReAct 循环**：规划步数 ≤3，规则优先。
3. **不做 A 股**：聚焦 HK + US。
4. **不给买卖指令**：合规红线，宪法约束。
5. **不编数字**：取不到就"未核到"，禁止估计范围/反推（R3 就是要把这条从提示词变成代码）。
6. **不在没有授权前把腾讯港股行情当商用数据源**（商业化阻断项）。
7. **不做过早抽象**：EA-6 等真实用户；R6 指引结构化等真实使用反馈；E7 名单可配置化等注册表需求。
8. **canary 不进 CI**：CI 保持无 key、不烧配额；真实探测属于本机/scheduler 的职责。
9. **涉及真实用户数据的 schema change 必须走非破坏性 migration**（D2 后定的规则；"清库重来"是当时数据库未上生产的一次性特例）。

---

## 5. 状态跟踪表

### 已建成（v1 全部里程碑，压缩记录——逐项验收细节见 git history 中的 v1 PLAN 与 git log）

| 轨道 | 内容 | 完成于 |
|------|------|--------|
| P0–P7 + 品牌 + 交互重构 | 工程门禁 / scheduler+通知 / 证伪监控 / 组合体检 / 画像时间线 / 前端模块化 / 发现层 / 港美一手管道 / Echo Research 品牌 | 2026-07-02 ~ 03 |
| EA-0…EA-5 | 统一入口 `/api/ask`、框架注册表 + 工具层、受控规划器、赛道词典 + 可解释排序、对话即容器 + 全标的进看盘、对话为中心前端（5 子阶段） | 2026-07-03 ~ 04 |
| B-1…B-7 | 置信度事实锚定、多期财报趋势、对比胜负手、风险雷达去占位、港股一手估值口径、HKEX PDF 复杂版式、web 证据真实实测（抓到搜索污染 bug） | 2026-07-04 |
| D1…D3 | 全量 JSDoc/checkJs 0 error（抓到 `hasPrice` 除数 bug）、`user_version` 迁移器 + 001_init.sql、chat 编排内化 `chatOrchestrator.js` | 2026-07-04 ~ 05 |
| C1（部分） | 375px 整页横向溢出阻断 bug 修复（`.sidebar`/`.desk` 补 `min-width:0`） | 2026-07-05 |

### 待办（本计划，待对齐）

| 阶段 | 名称 | 状态 | 备注 |
|------|------|:---:|------|
| G-1 | 数据质量面板 + 真实数据 canary（E1+E2+E3+P1） | ⬜ | 建议下一程第一步；产出健康面板 + 覆盖率度量 |
| G-2 | 财报日历与业绩事件锚（R1） | ⬜ | Finnhub 免费档需实测；港股缺日历时 fail-safe |
| G-3 | 估值同业锚 + 自动可比公司（R2+R4） | ⬜ | 历史分位免费档拿不到，先同业、诚实标缺 |
| E5 | typecheck 进 CI | ⬜ | 10 分钟顺手项，可随任意 PR |
| — | 第二程：R5 组合联动 / R3 数字级护栏 / E4 llm_audit / P2 通知回链 | ⬜ | G 轨完成后再对齐取舍 |
| — | P3 级：研究历史 FTS 检索 / C1 余项+C2 PWA / E6 缓存统一 / E7 名单可配置 | ⬜ | 记录在案，等时机 |
| — | P4 级：onboarding；远期：R6 指引结构化 / EA-6 / P8 | ⬜ | 明确缓做，条件见 §3 |

---

## 6. 怎么跑 + "完成"的定义（速查，承接 v1）

```bash
npm install                 # 只有 better-sqlite3 一个原生依赖
npm run seed                # 建/重置本地 SQLite 种子库
npm run dev                 # http://127.0.0.1:4173
npm test                    # 263 用例，必须全绿（EXIT=0）再提交
npm run lint                # eslint（correctness 级）
npm run typecheck           # tsc 全量 0 error（D1 成果，勿回潮）
npm run doctor              # 能力体检；--live 才发网络探活
# 后端无热重载！改 src/server/** 或 src/*.js 后要重启 node
# 隔离测试：LUVIO_DB_PATH=$TMPDIR/x.db PORT=4199 node server.js
```

**一次改动的"完成"= 代码 + 对应测试（进 `tests/`，接入 `npm test`）+ §5 状态表标 ✅ + 一条中文 commit。** 浏览器可见的改动必须实跑验证；涉及外部数据源的改动必须真实调用验证（B-7 教训）。代码改动走 branch + PR 进 main；纯文档改动可直接 commit main。

**key 都在 `.env`（gitignored）**：GLM / DEEPSEEK / FMP / FINNHUB / TWELVEDATA / TAVILY。沙箱会墙搜索引擎，端到端 web 证据效果要在用户本机验证。
